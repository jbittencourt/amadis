<?

class {LIB_NAME} extends CMObj {
  
  /*
   *This a description of the persistence class
   *of DB generated for the CMCLI
   *
   * private $query;
   *
   *public function configure() {
   *
   *  $this->setTable("table_name");
   *  $this->addField("field_name","field_type",size,notNull,default,auto_increment);  
   *  
   *  //sample
   *  $this->addField("cod", CMObj::TYPE_INTEGER,4,1,0,1);
   *
   *  $this->addPrimaryKey("cod");
   *}
   */

   public function configure() {
     /*
      *Its follows expecifications above
      */
   }
    

  /*
   * This is a method example that if executes
   * a query in DB an returns to controller it
   *
   *public function list(){
   * 
   * $this->query = new CMQuery({LIB_NAME});
   *
   * $ret = $this->query->execute();
   *
   * return $ret;
   *
   *}
   */

}
?>
